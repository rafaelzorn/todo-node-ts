generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "localhost"
  url      = env("DATABASE_URL")
}

model Todo {
  id          Int    @id @default(autoincrement())
  description String

  @@map("todos")
}
